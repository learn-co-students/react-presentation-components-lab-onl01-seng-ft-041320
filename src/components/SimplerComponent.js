import React from 'react';

const SimplerComponenet = (props) => {
    return (
        <div onClick={props.handleClick}>
            'I am just happy'
        </div>
    )
}

export default SimplerComponenet